# Models

Pure dataclasses describing the simulation state live here. The module covers
areas, characters, events, and `WorldState`, exposing helpers to load JSON data
from the `data/` directory or serialize snapshots for persistence/testing.

# CLI

Terminal presentation logic (app bootstrap + renderer) lives here. `app.py`
wires the controllers to user-facing flags, while `renderer.py` focuses solely
on formatting area overviews and turn transcripts.
